# API key required for all write operations (POST, PUT, DELETE)
# Set this in your environment before starting the server.
# In development, if unset, writes are allowed with a console warning.
# In production, if unset, the server will reject all write requests.
EMBERVAULT_API_KEY=your-secret-key-here

NODE_ENV=development
PORT=4000
